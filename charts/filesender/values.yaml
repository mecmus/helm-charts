replicaCount: 1
image:
  repository: ghcr.io/mecmus/filesender
  pullPolicy: IfNotPresent
  tag: "3.3-php"
service:
  type: ClusterIP
  port: 80
persistence:
  enabled: true
  size: 10Gi
  accessModes:
    - ReadWriteOnce
config:
  site_url: "http://filesender.lan"
  admin_email: "admin@example.com"
  db:
    type: "sqlite"
    host: "/var/www/html/db/filesender.sqlite"
    port: 0
    name: "filesender"
    user: "filesender"
    password: ""
  sso:
    enabled: false
    sp_entity_id: "http://filesender.lan"
    idp_entity_id: "https://sts.windows.net/TENANT_ID/"
    idp_sso_service_url: "https://login.microsoftonline.com/TENANT_ID/saml2"
    idp_certificate: ""
ingress:
  enabled: true
  className: "nginx"
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: "FASTCGI"
    nginx.ingress.kubernetes.io/fastcgi-index: "index.php"
  hosts:
    - host: filesender.lan
      paths:
        - path: /
          pathType: ImplementationSpecific
