apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "filesender.fullname" . }}-test-connection"
  labels:
    {{- include "filesender.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "filesender.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
